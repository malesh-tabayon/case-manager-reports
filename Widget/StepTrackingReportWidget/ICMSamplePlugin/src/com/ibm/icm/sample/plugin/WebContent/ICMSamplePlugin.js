var icmContextRoot = "/StepTrackingReportWidget";
var paths = {
	"steptrackingreport": icmContextRoot + "/steptrackingreport"
};
require({paths:paths});

require([
	"dojo/_base/declare",
	"dojo/_base/lang"
], function(declare, lang) {
	/**
	 * Use this function to add any global JavaScript methods your plug-in requires.
	 */

});
